# 📱 手机端流程控制台 · 项目交接文档 (HANDOVER.md)

> **交接时间**：2026-09-13  
> **当前设备环境**：Redmi Note 13 Pro (`a05b2a6c`, Android 16 / Xiaomi HyperOS)  
> **服务运行端口**：`http://127.0.0.1:8000` (FastAPI + Uvicorn 后台守护进程)  
> **数据库位置**：`storage/database/monitor.db` (SQLite)  
> **核心决策记录**：[DECISIONS.md](file:///d:/Akko的项目文件/手机端项目/DECISIONS.md) (所有设计拍板决策与推翻演进全记录)  
> **AI 研发铁律**：[AGENTS.md](file:///d:/Akko的项目文件/手机端项目/AGENTS.md) / [GEMINI.md](file:///d:/Akko的项目文件/手机端项目/GEMINI.md) (不可逾越的六大行为红线)  
> **存储索引清单**：[storage/INDEX.md](file:///d:/Akko的项目文件/手机端项目/storage/INDEX.md)

---

## 🏗️ 一、项目架构与规范化文件目录结构 (已完成清理)

项目根目录已完成全量杂乱临时文件清理（删除了 26 个散落的临时调试 XML、截图与 txt），归拢后的标准目录树如下：

```text
手机端项目/
├── HANDOVER.md                     # 🌟 本交接文档（新会话优先阅读）
├── DECISIONS.md                    # 🏛️ 核心架构方案决策与拍板记录
├── AGENTS.md / GEMINI.md           # 🤖 AI 助手核心研发守则与行为红线
├── README.md                       # 系统整体技术与操作说明文档
├── requirements.txt                # Python 依赖清单
├── start.bat / 一键启动网页.bat    # Web 运维监控看板快捷启动脚本
├── launch_web.py                   # 看板启动引导器
├── pdd_gpu_crawler.py              # 🛒 拼多多 5070Ti 16G 自动化采集主脚本 (高防风控+断点+嵌图)
├── bilibili_video_task.py          # 📺 B站当前在看视频防风控提取脚本 (双引擎模拟点击)
├── real_phone_automation.py        # 📱 真机 ADB 连通性与传感器遥测脚本
├── sample_mobile_task.py           # 🧪 模拟异常注入与巡检测试脚本
├── backend/                        # 后端核心运维引擎
│   ├── server.py                   # FastAPI 接口服务 (任务分发/断点查询/报表下载/配置管理)
│   ├── quality.py                  # 🛡️ 数据质量断言与统计学离群点质检引擎 (L1 本地规则/IQR箱线图)
│   ├── ai_auditor.py               # 🤖 DeepSeek AI 智能盲审探针引擎 (L2 会诊/根因溯源/脚本代码修改指导)
│   ├── item_replay_runner.py       # 📱 单品真实自动化回放沙箱探针 (详情页/规格抽屉/动态变价/存证截屏)
│   ├── probe_queue.py              # 📋 单品探针任务串行调度队列与调度管理器 (单例/真机防冲突/自定义排序/提前置顶/删除)
│   ├── runner.py                   # 异步任务执行器 (ScriptRunner) 与看门狗守护巡检器 (Watchdog)
│   ├── device.py                   # ADB 设备管理器 (带 2.5s TTL 缓存的高并发硬件遥测引擎)
│   ├── analyzer.py                 # AI 根因诊断引擎 (启发式专家规则 + OpenAI兼容大模型双模)
│   ├── notifier.py                 # 多渠道告警推送器 (飞书 / 钉钉 / 企业微信 Webhook 卡片)
│   ├── database.py                 # SQLite 存储层 (任务历史 runs、动态配置 configs 与 data_audits 质检表)
│   └── storage_indexer.py          # 全局存储扫描与 INDEX.md 自动索引生成器
├── frontend/                       # 前端交互控制台
│   └── index.html                  # 现代化响应式运维大屏 (TailwindCSS + Lucide，无需构建)
├── storage/                        # 🗄️ 集中持久化存储归档目录 (符合规范)
│   ├── INDEX.md                    # 自动生成的全量报表、快照、日志全局索引与体积统计
│   ├── README.md                   # 存储设计规范说明书
│   ├── temp/                       # 临时文件专用目录 (所有 XML dump 与中转截屏收拢在此)
│   ├── database/                   # 核心数据库文件 (monitor.db)
│   ├── reports/                    # 业务 Excel 报表与 JSON 备份
│   │   ├── pdd_gtx5070ti_16g_latest.xlsx  # 最新版 Excel 报表快捷访问副本
│   │   └── YYYY-MM-DD/             # 按日期分区的报告与断点存档
│   │       ├── pdd_crawler_checkpoint.json # 实时断点存档
│   │       ├── pdd_gtx5070ti_16g.xlsx      # 嵌入单元格快照的高清 Excel 报表
│   │       └── pdd_gtx5070ti_16g.json      # 全量结构化 JSON 数据
│   ├── captures/                   # 现场物理快照截屏库 (按日期分区)
│   │   └── YYYY-MM-DD/             # raw 高清原图 (2712x1220) 与 thumb 嵌入缩略图 (120x120)
│   └── logs/                       # 运维执行完整终端流日志 (按日期分区)
│       └── YYYY-MM-DD/             # run_{timestamp}_{hash}.log
├── bin/                            # 绿色便携版 ADB 工具目录
│   ├── adb.exe
│   ├── AdbWinApi.dll
│   └── AdbWinUsbApi.dll
└── scripts/                        # 辅助诊断与单项测试脚本库 (如 test_crawler_single.py 等)
```

---

## 🎯 二、已交付的核心功能与落地成果

### 1. 拼多多 RTX 5070Ti (16G) 自动化采集系统 ([pdd_gpu_crawler.py](file:///d:/Akko的项目文件/手机端项目/pdd_gpu_crawler.py))
- **采集规模**：默认前 **100 个** 有效商品（严格限定显存 16G，正则 `16g|16gb|16g显存` 校验）。
- **字段完整度**：包含序号、**单元格嵌入现场截图 (110x110)**、商品名称、价格、优惠详情、所属店铺、商品官方短链。
- **生物级防风控操作指纹**：
  - **触控高斯离散**：依据控件尺寸自适应离散坐标（留 6px 边距），杜绝固定点敲击；
  - **贴屏按压时长**：以 `swipe` 瞬时微动模拟 `75ms ~ 145ms` 真实手指肉垫接触时长；
  - **单手握持滑动**：起始落点离散，带有 `-35px ~ +30px` 人体工学横向摆动与 330~480ms 速度变化；
  - **拟人驻留与微交互**：进店随机等待 `2.5s ~ 4.8s`，25% 概率执行轻触微滑查看详情，每 8~12 个商品触发 `4.5s ~ 7.8s` 思考停顿。
- **翻页重叠优化**：滑动距离严格控制在 **~750px（半屏滑动）**，保持 50% 画面重叠，彻底消除滑过漏采。
- **断点继跑 (Checkpoint Recovery)**：
  - 每采集 1 个商品实时落盘至 `pdd_crawler_checkpoint.json`；
  - 遇到异常中断，下次运行自动识别历史进度，自动跳过已有项，序号与 Excel 表格平滑续接。
- **核心调度三参数设置（品类 / 数量 / 排序）**：
  - **1. 采集品类 (搜索词)**：前端提供输入框与常用快捷药丸标签（`RTX 5070 Ti`、`RTX 4060`、`iPhone 16`、`电竞显示器`、`机械键盘`），脚本动态切换检索词；
  - **2. 目标采集数量**：支持输入与药丸步进（`20项 (快速)`、`50项`、`100项 (标准)`、`200项`），按需决定采数规模；
  - **3. 搜索排序规则**：支持 `🔥 综合推荐`、`📈 销量优先`、`💰 价格优先`，脚本进入结果页后自动在真机点击对应 Tab 切换排序；
  - **持久化与自动保存**：前端输入 600ms 防抖自动存盘入库 (`configs` 表中的 `pdd_crawler_params`)，页面刷新无感回显。
- **全方位自愈与防误触体系**：
  - 点击前执行 Android 底层 `service call statusbar 2` 强行收起悬浮通知横幅；
  - 点击后实时校验前台窗口，若误触外部消息（微信、短信等）立即自动连续返回退出并切回拼多多，跳过该项防脏数据；
  - `return_to_search_results()` 具备多轮重试与自愈恢复，保证 100% 停留在搜索流。

### 2. B站当前在看视频防风控提取系统 ([bilibili_video_task.py](file:///d:/Akko的项目文件/手机端项目/bilibili_video_task.py))
- 无侵入视觉树提取（UI Hierarchy），不需要发包也不触发风控；
- 成功提取视频标题、UP主、粉丝数、播放量、弹幕数、在看人数、三连互动数据及当前播放分集；
- 独创 **Monkey DispatchPointer 物理点击降级引擎**，完美绕过小米 HyperOS 的 `INJECT_EVENTS` 权限拦截。

### 3. Web 自动化运维监控看板与 AI 诊断
- **仪表盘首页**：访问 `http://127.0.0.1:8000`，实时查看脚本成功率、平均耗时、红米手机传感器遥测（电量、电池温度、前台应用）；
- **执行控制台**：一键派发任务、选择场景、切换断点续跑、一键下载带截图的 Excel 报表；
- **排查详情与 AI 根因**：捕获报错瞬间的高清截屏，规则/大模型定性分析并给出运维排查行动建议；
- **告警通知闭环**：支持飞书、钉钉、企业微信机器人 Webhook 联动。

### 4. 数据质量防御与智能盲审中枢 (三层拦截防御 + 多脚本文件夹分类工作台)
- **核心痛点解决**：彻底攻克“流程执行 100% 成功无报错，但商家标题塞入 12G/16G 复合引流关键字导致抓取到低配 12G 起步价”的致命静默业务脏数据陷阱，同时彻底解耦单一脚本死板绑定的问题。
- **三层拦截防御体系**：
  1. **Layer 1: 本地规则与统计学滤网 (0 Token 成本，毫秒级流式初筛)**：在 `backend/quality.py` 中运用**标题多规格冲突惩罚**与**批次 IQR 箱线图离群点检测**，毫秒级自动放行 ~88% 的合规干净数据，仅把 ~12% 的存疑项打上标签送审。
  2. **Layer 2: 单品步骤回放探针与 DeepSeek AI 智能确诊 (`backend/item_replay_runner.py` + `backend/ai_auditor.py`)**：坚决推翻“无客观依据的纯文本猜想”。仅针对 L1 标记的模糊存疑项，自动由探针针对单品独立重跑采数全流程（Step 1 首屏标价 ➔ Step 2 抽屉展开识别商家默认勾选低配 12G ➔ Step 3 点击目标 16G 捕获动态变价），生成 3 步客观实况截图与结构化证据链喂给 DeepSeek。大模型结合变价前后实况对比，得出 98% 高置信度确诊（`CONFIRMED_FRAUD_CORRECTED`），精准推导真实 16G 纠偏价（如 ¥9899/¥10299）并输出可直接落地的 RPA 代码修复建议。
  3. **Layer 3: 人机极速协同与纠偏直达纯净 Excel (`frontend/index.html`)**：在 Web 界面不仅呈现 AI 诊断卡片与 3 步实景快照，还在操作区生成专属绿色按钮 **「按纠偏价¥XXXX通过」**（支持快捷键 `[空格]` 一键采纳），无需人工手动改价即可将真实价格沉淀入库，并直达导出的纯净版 Excel 报表！
- **多脚本“文件夹/文件柜式”分类工作台**：
  - 像操作系统文件夹一样组织任务：`全部任务总览`、`🛒 [电商采数] 拼多多-RTX5070Ti`、`📺 [社交监控] B站UP主视频数据`、`📱 [系统运维] 移动端权限与自愈`；
  - 各脚本拥有独立的审核队列、指标统计大盘、AI 会诊历史，并支持 `GET /api/reports/{script_name}/excel-clean` 分任务导出纯净版 Excel。

---

## 💡 三、关键坑点与解决方案备忘录 (必读)

1. **Windows 下 ADB 拉取中文路径崩溃 Bug**：
   - *现象*：`adb pull /sdcard/... "d:\Akko的项目文件\..."` 会在 Windows CMD/PowerShell 下抛出 `UnicodeDecodeError: 'gbk' codec can't decode...`。
   - *解法*：必须先拉取到纯 ASCII 路径（如 `storage/temp/rel_window_dump.xml`），再用 Python 的 `shutil.copy2` / `Pillow` 保存到中文项目路径。
2. **拼多多“复制链接”按键双击导致退出 NewPageActivity Bug**：
   - *现象*：点击“复制链接”后弹窗会自动消失，若无脑调用两次 `keyevent 4`，会导致直接退出了搜索结果流回到首页推荐流。
   - *解法*：使用 `return_to_search_results()` 实时 dump 判定当前是在详情页、首页还是搜索页，动态决定返回策略。
3. **消息弹出误触导致跳出拼多多 Bug**：
   - *现象*：微信或系统通知悬浮弹出时，脚本点击操作会点中通知横幅进入外部应用。
   - *解法*：点击前调用 `service call statusbar 2` 收起通知栏；点击后通过 `get_current_window()` 核验是否是拼多多，若误入则立即自愈脱困并跳过该项。

4. **控制台清空断点从头开始仍显示“已采集 100 项”缺陷 (已修复)**：
   - *现象*：采完 100 项后用户在 Web 页面点击「清空断点从头开始」，界面仍显示“已采集 100 项”。
   - *根因*：`/api/crawler/checkpoint` 拥有 fallback 逻辑，原 reset 仅删除 `ckpt_file`，导致接口和爬虫回退读取到了同目录下包含 100 条旧数据的 `pdd_gtx5070ti_16g.json`。
   - *解法*：重置时自动将旧 json 归档为 `_archived_{timestamp}.json` 切断泄漏（确保数据不丢），写入显式 `status: "RESET"` 凭据，并在前端对齐全新就绪态 `✨ 全新采集 (0/100)` 渲染。

5. **控制台启动脚本手机无反应且日志卡死问题 (已彻底修复)**：
   - *现象*：在控制台点击「立即执行测试脚本」后，弹窗显示“执行中/0s”，但手机未响应，控制台输出流空白，任务卡死。
   - *四大根因*：
     1. `backend/runner.py` 子进程启动时漏传 `-u` 参数与 `PYTHONUNBUFFERED=1` 环境变量，导致 Windows 管道默认开启 4KB 块缓冲，前序几十行关键输出全被压制在缓冲区无法流式送达前端；
     2. 缺少 `cwd=PROJECT_ROOT` 与 `stdin=subprocess.DEVNULL`，造成子进程在 Windows 后台托管时对控制台句柄等待阻塞；
     3. `device_manager` 的定时遥测以 2.5s 高频轮询 dumpsys，且在超时 4s 时粗暴执行 `taskkill /F /IM adb.exe`，直接斩断了爬虫脚本刚建立的 ADB 链路，造成 ADB 通讯死锁；
     4. `is_busy()` 死锁：异常挂起的进程占用内存句柄且未支持前台中止，阻塞后续所有触发。
   - *闭环解法*：
     1. 子进程注入 `sys.executable -u`，配置 `PYTHONUNBUFFERED=1`、`PYTHONIOENCODING=utf-8`、`PYTHONUTF8=1` 环境变量；
     2. 设备管理器引入 `set_script_running` 独占感知：自动化脚本执行中立即挂起重度 dumpsys 轮询，返回缓存遥测，严禁任何 `taskkill adb.exe`；
     3. 服务端与前端控制台新增「中止任务」功能（`/api/runner/stop` 与 `/api/runner/cancel`），支持随时从网页强行终止任务；
     4. 服务端冷启引入 `cleanup_stale_running_tasks()`，自动收敛未闭环的孤儿 RUNNING 记录。

6. **长流程临时断开手机导致数据丢失与误告警**
   - *现象*：采集中途用户拔出手机充电或外出，强行终止导致 ADB 损毁报错并误触发告警，已采集的几十条数据未生成 Excel。
   - *闭环解法*：
     1. 抽象出独立的 `PAUSED` 任务状态，与 `FAILED` 彻底解耦；
     2. 爬虫循环中引入 `crawler_pause.signal` 信号检测与 `KeyboardInterrupt` 拦截，退出前将断点写回 `pdd_crawler_checkpoint.json`，并导出已有数据的阶段性 Excel 与 JSON；
     3. 控制台添加「暂停并存盘」按钮与 `/api/runner/pause` 接口，暂停后释放 ADB 独占锁，手机可直接拔出；重连后界面高亮金黄色断点提示，开启「断点续跑」即可直接从第 N+1 项继续。

7. **搜索列表翻页无限重置回顶与输入框盲打污染死锁 (已彻底修复)**：
   - *现象*：断点续跑或向下滑动多屏后，脚本突然点击搜索框重新搜索，导致浏览进度重置回第 1 项；在输入框中拼写混乱叠字，且易把取消误当弹窗关闭。
   - *三大根因*：
     1. 页面检测依赖文本关键字 `"5070" in all_texts`。当向下滑动多页遇到平台穿插推荐杂项（如纸巾、其他型号显卡）时，当前视野无 5070 字样被误判为脱轨，误触搜索框重搜回顶；
     2. 翻页无新增项（如过滤旧数据时）与自愈强耦合，直接触发 `ensure_in_search_results` 陷入死锁循环；
     3. 输入框使用硬编码绝对坐标盲打，且未清空残留文字。
   - *闭环解法*：
     1. 引入**全域页面确定性状态机** `detect_current_page(xml)`，支持六大页面类型（结果列表、商品详情、SKU抽屉、搜索输入、首页、弹窗），判定准确率 100%；
     2. 解耦翻页与自愈：处于结果列表时只管顺畅向下滑动，不再受屏幕文本是否有特定关键词干扰；
     3. 搜索输入重构：优先点击历史搜索标签（0 键盘冲突）；若无标签则动态定位 `EditText`，使用底层按键码清理输入并触发原生回车提交；
     4. 详情页退出使用左上角返回按键真实坐标，消除大图误触。已顺利支撑全量 100 项连续稳定采集。

8. **100% 物理真机单体验证探针与假数据零容忍治理 (已彻底闭环)**：
   - *现象*：用户点击 L2 AI 诊断卡片的多步缩略图时发现存在 PIL 绘制的暗色占位矩形假图与公式假纠偏价。
   - *根因*：旧版 `backend/item_replay_runner.py` 曾用 PIL 虚拟绘图代码冒充真机截屏，违背了证据驱动与零盲猜铁律。
   - *闭环解法*：
     1. 完全删除 PIL 绘图代码与假公式，探针必须 100% 调度已连接物理真机（Redmi Note 13 Pro）；
     2. 修复 `run_adb` CompletedProcess 对象解包异常，并在规格项点击时采用原生 `input tap`，杜绝 `swipe` 触控时长过长被 ScrollView 拦截误判为滑动；
     3. 真实物理驱动 3 步原子存证：
        - 步骤 1：真机直达商品详情页，抓取 5MB 真实原图 `step1_detail.png`，提取首屏标价与店铺；
        - 步骤 2：真机触控展开规格抽屉，抓取真实原图 `step2_sku_drawer.png`，解析真实 SKU 列表与默认选中项；
        - 步骤 3：综合品牌与系列型号打分定位目标 16G 规格，真机点击卡片后抓取变价原图 `step3_selected.png`，读取联动真实售价；
     4. 物理实测数据（如 ¥9799、¥9299）拥有绝对真理权重，DeepSeek 消费真实证据链后产出 98% 高置信度会诊；
     5. 实测多案例真实闭环：
        - 条目 #13 (ID 120 盈通 5070Ti)：证实 12G 售罄，默认即为 16G 真实价格 ¥9299，核准通过 (PASS)；
        - 条目 #27 (ID 134 技嘉 5070/5070Ti)：证实首屏 ¥7299 系 12G 万丽雪狐引流，抽屉选中技嘉风魔 16G 后变价为 ¥9799（差价 +¥2500，`selected: true`），完成真实纠偏；
        - 条目 #39 (ID 146 微星 5070Ti)：真实真机端到端全流程自动重跑，展开抽屉选中【5070Ti 黑火神X OC 16G】后从 ¥7699 跳变至 ¥11790（差价 +¥4091），生成全真实原画与 DeepSeek 98% 纠偏；
        - 条目 #23 (ID 130 映众 5070Ti)：用户反馈排查后重测，真机精准搜索并捕获微星万图师 16G 售价 ¥9396，替换为 4.3MB 真实手机原画。
     6. 历史遗留假图彻底清零：磁盘上清理删除全部 10 个遗留的 ~37KB 假图目录（audit_106/187/198/29/49/56/60/68/7等），数据库对应行重置为 `NOT_RUN`。

9. **脱离 AI/Agent 辅助的全自主真机自闭环 (Zero Agent Reliance & Full Web Autonomy)**：
   - *现象*：用户脱离 AI 会话独立在浏览器测试单品诊断时，手机未执行动作，返回了旧版内存里的预制结果（“追风 12G ¥10299”）。
   - *根因*：原后台 Uvicorn 进程在代码重构前启动且未带 `--reload`，常驻内存锁定了旧版代码，请求击中旧进程直接返回了假数据。
   - *解法*：
     1. 彻底杀灭旧常驻进程，`launch_web.py` 全局注入 `--reload` 热更新机制；
     2. 经 HTTP REST API 端到端验证：触发 `/api/quality/audits/274/replay-probe`（商品 #67 万丽 5080/5070Ti），真机物理全自动运行（耗时 53.8s），搜索商品、进入详情、展开抽屉、点击 16G、生成 3 张真实高清原画（每张 4MB+），捕获真实纠偏价格 ¥9396（原标价 ¥7149），并由 DeepSeek 出具高置信度报告；
     3. 连同条目 #45 (ID 252)，全部达成脱离 AI 助手全自主运作。任何人在浏览器点击按钮，手机都会真实运转。

10. **中文搜索词崩溃与首项卡片盲点缺陷彻底根治 (URI Scheme Navigation & Weighted Card Scoring)**：
    - *现象*：用户在对不同商品（如万丽星际、万丽星云、技嘉风魔等）发起 L2 真实单品重跑探针时，手机虽然唤醒，但每一个商品都固定搜出同样的结果并点击第一张微星万图师卡片，导致所有 AI 检查截图完全雷同。
    - *根因*：
      1. `adb shell input text` 无法直接处理中文字符，抛出 `java.lang.NullPointerException`，导致搜索框从未输入具体商品关键词，一直残留旧泛搜词；
      2. 搜索结果页由于残留泛词找不到目标品牌，代码无脑 fallback 至 `cards[0]` 点击首项卡片；
      3. 规格抽屉顶部常驻毫秒倒计时动画（`01:39:48.1 内下单`），导致 `uiautomator dump` 因无法进入 Idle 状态而报错 `could not get idle state` 超时；旧版 `dump_ui` 未清理手机端旧文件，超时后误 pull 了上次微星的 XML 缓存。
    - *解法*：
      1. **官方 URI Scheme 直达**：改用 `pinduoduo://com.xunmeng.pinduoduo/search_result.html?search_key={quote(kw)}`，0 字符输入、原生支持 URL 编码中文直达精准搜索页；
      2. **四因子加权卡片匹配算法**：品牌 (+40分)、次级系列 (+50分，如星际/星云/雪狐/风魔/魔鹰等)、核心芯片 (+20分)、引流起步价邻近度 (+30分)，精准锁定最高分卡片大图，彻底废除 `cards[0]` 盲点；
      3. **`dump_ui` 防脏数据闭环**：每次 dump 前强制清除手机与本地历史 XML；抽屉若遇动画阻断则由真实屏幕实拍存证 + 详情标题型号判定，确保 100% 真实真机事实闭环；
      4. **连续 3 次返回收尾**：探针结束时自动连续 3 次 `keyevent 4` 退出抽屉与详情页，手机界面始终保持整洁。
    - *端到端验证*：
      - 条目 #67 (万丽星际)：精准命中万丽星际卡片，抽屉实测价格 ¥7149，生成 3 张真实高清截图；
      - 条目 #45 (万丽星云)：精准命中万丽星云卡片，抽屉实测为 12GB 5070 (¥7149)，精准查出商家标题堆叠 16GB 虚标引流；
      - 条目 #27 (技嘉风魔)：精准命中技嘉风魔卡片，抽屉实测仅售 RTX 5050 8G (¥2899)，判定为 `FAILED_SKU_ABSENT` 虚标低配引流。
   4. **跨品牌张冠李戴与低配 12G 假放行防御踩坑与解法**：
      - **致命踩坑 1**：部分商家在标题中堆叠 `16GB` 与 `5070Ti`，实际规格抽屉在售仅为 `12G`。若抽屉未找到 16G 按钮而简单回退信任商家标题，会导致 12G 被误当做 16G 放行！
        - **解法**：深入抽屉后调用 `RapidOCR` 对截图做全文字符扫描，凡检测到 `12G`、`12GD7`、`12GB`、`8G`、`5060` 且无 16G 选项者一律确立为 `FAILED_SKU_ABSENT`，纠偏价置为 `null`，终审裁决为 `FRAUD_EXCLUDED`，彻底杜绝假放行。
      - **致命踩坑 2**：搜索词过于简陋（如将 `【耕升】映泰RTX5070TI 16G风火凤凰` 缩水为 `耕升 5070Ti`）会导致拼多多推荐微星/映众广告卡片，且缺乏品牌强过滤时会点击错误品牌的卡片。
        - **解法**：
          1. 扩充全品牌（21个）与次级系列（45个）词库，生成高辨识度多特征检索词（`耕升 映泰 5070Ti 凤凰`）；
          2. 引入**一票否决制品牌守卫 (Brand Guard)**：非目标品牌卡片直接赋予 `-9999` 分，原抓取标价强吻合 (+800分)；
          3. 详情页执行品牌一致性断言，若进入非目标品牌商品，立即触发 `FAILED_MISMATCH_BRAND` 熔断拦截。
    5. **SKU / goods_id 确定性直达与双轨导航架构**：
       - **架构洞察**：关键词搜索永远存在动态排名、竞价广告和个性化推荐干扰。电商的最底层确定性坐标是 SPU（`goods_id`）与 SKU（抽屉款型）。
       - **解法**：
         1. **零干扰提取**：通过 `dumpsys activity top | grep -o goods_id=[0-9]* | head -n 1` 从安卓栈直接提取当前打开商品的 `goods_id`，采数主脚本自动存入链接；
         2. **URI Scheme 穿透**：探针优先检查 `goods_id`，直接唤起 `pinduoduo://com.xunmeng.pinduoduo/goods.html?goods_id={goods_id}` 0.5 秒直达目标详情页，彻底绕过搜索框；
         3. **双轨兼容**：有 `goods_id` 走直达通道，无 `goods_id` 自动降级为品牌守卫高精多 Token 检索。
    6. **拼多多内部搜链接直达与 SQLite 全流程持久化**：
       - **用户指导最佳实践**：在拼多多内部直接将商品真实短链（带 `ps=...` 或 `goods_id=...`）作为搜索词提交，拼多多内部搜索系统会直接穿透拉起对应商品详情页，彻底消灭搜索列表广告与店铺张冠李戴。
       - **规避系统浏览器**：严禁调用系统级 `am start -d "https://..."`（会被 HyperOS 等手机系统拦截跳转外部浏览器），必须严格调用拼多多内部搜索协议 `pinduoduo://com.xunmeng.pinduoduo/search_result.html?search_key={urllib.parse.quote(link)}`。
       - **SQLite 完整入库闭环**：采数主流程在进入详情页时自动捕获商品真实短链，写入 `item_data["link"]` 并通过 `save_audit_batch` 100% 固化到 SQLite `data_audits`，探针重跑直接取该链接在内部搜索直达原店铺原商品。
    7. **多品类采数防崩溃与原生 URI Scheme 搜索直达 (已彻底加固)**：
       - **UI Dump 零崩溃防御**：真机繁忙时 `dump_ui` 自动写入合法最小 XML，配合 `safe_parse_xml` 兜底，彻底杜绝 `search_page.xml` 的 `FileNotFoundError` 崩溃。
       - **首页 Feed 冒充搜索列表根治**：`PAGE_HOME` 增加底部大导航栏 Tab（y > 1800）特征识别，必须在无底部 Tab 且具有综合/销量排序栏时才判定为 `PAGE_SEARCH_RESULTS`。
       - **官方 URI Scheme 直达搜索**：`ensure_in_search_results` 直接调用 `pinduoduo://com.xunmeng.pinduoduo/search_result.html?search_key={encoded_kw}`，2.5s 直达搜索列表，彻底弃用首页脆弱盲点。
       - **全品类关键词强特征过滤**：`get_visible_cards` 对非 5070 品类提取强特征 token（如 `iphone`, `16`, `4060` 等），标题未命中直接过滤，彻底杜绝搜手机采集到纸巾的品类漂移。
       - **0.12s 极速活跃 Fragment goods_id 直取**：改用 `dumpsys activity com.xunmeng.pinduoduo`（耗时仅 0.12 秒），严格约束仅在 `mResumed=true mStopped=false` 的 `ProductDetailFragment` 处于前台时提取，杜绝复用后台历史遗留 ID。
       - **自适应动态中位数价格质检**：非 5070 商品基于当前批次中位数动态自适应 `[median * 0.65, median * 1.45]`，避免通用商品被硬编码 7800 元误拦截。
   - **多探针真机并发冲突与串行任务调度引擎 (`ProbeQueueManager`)**：
         - **物理真机冲突痛点**：真机物理资源唯一（ADB 通道、触控输入、UI Automator 窗口转储为独占资源）。若多个探针并发运行，会互相干扰导致按键错位、点击冲突或任务意外被挤掉。
         - **单例串行调度引擎 (`backend/probe_queue.py`)**：通过后台单一工作线程循环驱动，实现任务互斥独占排队（同一时刻真机绝对仅跑 1 个探针），新任务安全入队保护（Zero-Eviction），杜绝被打断或挤掉。
         - **全功能调度管理闭环**：支持「⚡ 提前任务 (置顶插队)」、「↕️ 自定义排序 (上移/下移/全量重排)」、「🗑️ 删除任务」、「清空等待队列」、「⏹️ 强行中止当前任务」，并通过前端「📋 探针任务管理」控制台及盲审卡片联动呈现秒级实时状态。
   - **非显卡品类质检误杀根治与意图编译型 Agent 体系 (Intent-First Quality Architecture)**：
         - **踩坑根因**：旧版质检引擎在 `calculate_batch_stats` 中硬编码 `if p and p > 1000:`，导致快消品（如 50~100 元的珠江啤酒）价格全部被过滤，大盘退回 9499.0 元显卡兜底价；随后在 `audit_item` 触发 `price_val < 7500.0`，强行打上“极高概率12G引流”误判为 REJECT；
         - **规则编译型 Agent (Zero-Runtime Cost)**：在配置期介入 1 次，将用户的自然语言质检诉求（如“只要珠江品牌，排除定金”）编译为结构化策略 `rule_profile`，运行时完全离线以 0 成本、毫秒级执行；
    - **动态 Token 榜单直达与详情页专用返回防穿透踩坑备忘 (`pdd_beer_top15_crawler.py`)**：
          - **踩坑 1（动态 Token 封禁）**：直接通过 Scheme 唤起排行榜会被拼多多以 `list_sn` 过期为由拦截并提示“该榜单已下架”；解法：先进入上榜单品详情页，点击客户端向服务端换签生成的动态药丸胶囊，100% 鲜活入榜；
          - **踩坑 2（全局 Back 穿透退回搜索/桌面）**：使用 `input keyevent 4` 容易连击关掉底层榜单；解法：物理轻触详情页左上角的专用返回按钮（坐标 `75, 190`），仅出详情页栈，配合 `check_back_status` 自愈重连；
          - **踩坑 3（详情页底部推荐商品污染标题）**：详情页长文本比较容易命中底部推荐流；解法：标题价格与榜单卡片信息严格锚定，价格约束在 `Y < 1600` 区域提取；
          - **实机全量交付成果**：2026-09-17 完整跑通 Top 15 啤酒单品全量采集，生成嵌入 15 张现场主图缩略图的高清 Excel，并通过 ADB 成功推送到手机内部存储 `/sdcard/Download/拼多多啤酒畅销榜Top15_{DATE}.xlsx`。
     - **RPA 任务质检定制 SOP 技能沉淀 (`.agents/skills/rpa-task-quality-customizer/`)**：
           - **核心定位**：针对用户拍板的“任务按需定制、独立文件柜管理面条代码、三层防御骨架统一”原则，沉淀标准 Antigravity Skill；
           - **核心资产**：
             1. `SKILL.md`：标准化 7 步作业流程（意图定界 ➔ 独立文件隔离 ➔ L1极速规则 ➔ L2真机物理探针 ➔ L3终审导出 ➔ UI自适应 ➔ 0残留回归）；
             2. `references/quality_archetypes.md`：四大通用质检模态（电商防引流、榜单连续性、社交互动监控、系统健康巡检）速查手册；
             3. `examples/evaluator_template.py`：L1 规则评估器标准示例代码；
             4. `examples/probe_template.py`：L2 物理真机单体探针标准示例代码。
     - **商品详情页误判排除与就地胶囊跃迁优化 (`pdd_beer_top15_crawler.py`)**：
           - **踩坑（误判假死）**：旧逻辑单凭文本包含 `"品牌罐装啤酒"` 判定榜单，由于商品详情页有 `第4名 | 品牌罐装啤酒降价榜` 胶囊，导致脚本将详情页误当成榜单页，在详情页寻找 `热卖指数` 失败引发空转退出；
           - **解法 1（详情页排除）**：`is_in_beer_chart` 显式过滤商品详情页特征词（`立即购买`、`立即拼单`、`小直播中` 等）；
           - **解法 2（胶囊就地跃迁）**：若真机停留在详情页，直接识别并轻触 `try_jump_from_capsule` 入榜，且搜索进入时去掉 `-S`，避免杀进程冷启动与开屏弹窗；
           - **解法 3（下边缘边界保护）**：`extract_beer_items_from_chart` 过滤 `card_top > 2100` 或 `click_bounds[3] > 2450` 的半截卡片，杜绝点击屏幕外部手势区。
      - **确定性 goods_id 穿透主轨与原生触控事件注入架构 (`pdd_beer_top15_crawler.py`)**：
            - **踩坑（搜索页遮挡与 Monkey 0延时丢触控）**：搜索关键词后滑屏过量导致卡片被顶部筛选栏覆盖，且 `fl_float_sug` 搜索浮层干扰；此外无时延 Monkey 脚本易被底层过滤且命中偏左文字；
            - **解法 1（龙头单品 goods_id 穿透主轨）**：通过常年霸榜单品 `BEER_LEADER_GOODS_ID = "709492586641"` 唤起官方 Scheme，0 误差直达详情页再点胶囊；
            - **解法 2（原生系统触控）**：全面采用系统级 `adb shell input tap x y`，胶囊热区精准定位在中轴中心 `(610, 2263)`；
            - **解法 3（实机极端冷启动验证）**：进程强杀状态下 34 秒自主完成冷启动唤醒入榜，端到端 100% 成功采集 Top 15。
      - **工程架构瘦身与历史测试资产治理 (`clean_project_assets.py`)**：
            - **治理成果**：依照“三除三保”原则，彻底清除 184 个独立垃圾文件、17 个历史废弃目录，释放 **1.07 GB** 空间；
            - **数据库重组**：清洗 2026-09-16 之前的 135 条旧测试脏数据，执行 VACUUM 将 `monitor.db` 紧凑至 80KB；
            - **资产保护**：严格保护 5 大核心脚本、后端调度与 9.16~9.17 的高质量带图 Excel 成果。
      - **多任务质检定制 SOP 与盲审中枢看板动态感知重构 (`rpa-task-quality-customizer` & `index.html`)**：
            - **踩坑（显卡魔数全盘硬编码与跨品类污染）**：旧控制台在顶部基准固定写死 `大盘中位数: ¥9,989`，拦截指标卡固定为 `拦截低配引流`，导致切换到啤酒榜单时仍试图高亮 12G/16G 并显示显卡中位数；
            - **解法 1（SOP Skill 沉淀）**：沉淀标准技能 [`.agents/skills/rpa-task-quality-customizer/SKILL.md`](file:///d:/Akko的项目文件/手机端项目/.agents/skills/rpa-task-quality-customizer/SKILL.md)，规范 4 类质检原型（`SPEC_FRAUD`, `RANK_HOTNESS`, `SOCIAL_METRIC`, `SYSTEM_TELEMETRY`）与文件隔离开发模板；
            - **解法 2（后端契约扩展）**：`database.py` 动态聚合 `l1_rule_breakdown`，实时统计每个任务触发的具体规则排行，并实算真实 `value_context`（如啤酒标价中位数 ¥45.0）；
            - **解法 3（前端看板 100% 结构保留与动态插值）**：Card 2 动态渲染 L1 规则拦截胶囊，大盘基准动态展示实际指标，表格表头、高亮与探针文案解耦，非显卡任务残留为 0。

---


## 🚀 四、接手人员快捷运行指令

```bash
# 1. 启动 Web 运维监控看板后台服务 (已在后台运行，若需手动重启执行以下命令)
python -m uvicorn backend.server:app --host 127.0.0.1 --port 8000

# 2. 命令行执行拼多多品牌啤酒畅销榜 Top 15 采集 (断点续跑)
python pdd_beer_top15_crawler.py --count 15 --resume

# 3. 命令行全新重采啤酒畅销榜 Top 15 (清空历史断点)
python pdd_beer_top15_crawler.py --count 15 --fresh

# 4. 命令行断点续跑拼多多显卡采集任务 (默认自动续跑)
python pdd_gpu_crawler.py --resume --count 100

# 5. 命令行全新从头采集显卡 (清空断点缓存)
python pdd_gpu_crawler.py --fresh --count 100

# 6. 执行全盘存储容量统计与 INDEX.md 索引更新
python backend/storage_indexer.py

# 7. 工程资产清理与数据库瘦身整理 (带 --execute 真实执行)
python clean_project_assets.py --execute

# 8. 一键构建脱敏发布交付包与三合一独立项目压缩包
python build_release.py
```

---

## 🔮 五、下一会话建议推进的功能规划 (Roadmap)

根据业务评审，建议在后续开发中优先实现以下功能：

1. **【第一优先级】APScheduler 定时任务调度器**：
   - 支持设置标准 Cron 表达式（如每天凌晨 02:00 自动触发拼多多 100 项采集）；
   - 增加 FIFO 任务等待队列，避免任务并发被拒绝。
2. **【第一优先级】影刀 (ShadowBot) 与 实在智能 脚本监控探针**：
   - 探针模块监测电脑上 `ShadowBot.exe` / `IS-Robot.exe` 进程的启动与退出；
   - 监听 `%APPDATA%\ShadowBot\logs`，一旦影刀报错自动抓取错误流并接入现有 AI 根因诊断看板。
3. **【第二优先级】告警风暴抑制与降噪机制**：
   - 10 分钟内相同故障类型仅发送 1 次 Webhook 报警；
   - 故障自愈后发送绿色恢复确认通知。
4. **【第三优先级】Web 网页端实时投屏 (Web-Scrcpy)**：
   - 看板内嵌实时手机投屏画面，支持运维人员在网页上远程滑动处理滑块风控验证码。
5. **【第四优先级】高清截图生命周期自动清理**：
   - 超过 7 天的历史原画截图（`*_raw.png`）自动清理，永久保留几 KB 的 Excel 缩略图（`*_thumb.png`）。
